[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pycraft"
version = "0.1.0"
description = "PyCraft Python API for Minecraft"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["pycraft", "pycraft.api"]